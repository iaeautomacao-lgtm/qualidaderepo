import PainelGestao from "../page-base";

/** Gestão — aba Usuários (gestão de usuários e atividade recente de login). */
export default function GestaoUsuariosPage() {
  return <PainelGestao abaInicial="usuarios" />;
}
